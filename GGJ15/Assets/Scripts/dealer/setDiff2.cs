using UnityEngine;
using System.Collections;

public class setDiff2 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		PlayerPrefs.SetInt("difficulty", 2);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
